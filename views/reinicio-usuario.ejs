<%- include('includes/head.ejs') %>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/login.css">

    </head>

    <body>
        <%- include('includes/navigation.ejs') %>

            <main>
                <% if (mensajeError) { %>
                    <div class="user-message user-message--error">
                        <%= mensajeError %>
                    </div>
                    <% } %>
                        <div class="box-center_flex">
                            <form class="container" action="/usuario/reset-password" method="POST">
                                <div class="datos">
                                    <legend class="legend">
                                        <div>Recuperación de contraseña</div>
                                    </legend>
                                    <div>
                                        <label>Correo electrónico</label>
                                        <input type="email" name="email" class="inputText" autocomplete="off" required>
                                    </div>
                                    <div>
                                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                        <button type="submit" class="btn-samsung">
                                            <span>Recuperar contraseña</span>
                                        </button>
                                    </div>
                                </div>
                            </form>
                        </div>

            </main>
            <%- include('includes/end.ejs') %>